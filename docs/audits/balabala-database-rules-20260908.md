# 巴拉租户数据库规则核对

北京时间2026-09-08 16:59:45，通过堡垒机在 Listingify 运行中的 `listingfy-api` 容器使用现有数据库连接执行只读、可重复读事务；数据库返回 `transaction_read_only=on`。未修改数据库、凭据、商品或业务代码。

- 精确范围：`tenant_name=电商巴拉巴拉 AND merchant_id=1162`。
- `deepdraw_field_mapping_rule`：209行，全部enabled=true，涵盖13个字段域。
- `product_archive_field_rule`：0行（此表没有租户列）。
- 209条规则明细见同目录 `balabala-database-rules-20260908.csv`。

## 修正此前结论

此前仅查仓库未查数据库，误将下列字段归为未找到规则/待补资料。现在已经取得实时数据库证据，应归为 CLI 规则接入缺口。

| 规则ID | 字段 | 数据库默认值 | 当前本地模板经Listingify来源解析/归一后的结果 |
| --- | --- | --- | --- |
| 20 | 尺寸推荐表 | 禁用 | 三款服饰均生成文本“禁用” |
| 21 | 试穿报告表 | 禁用 | 三款服饰均生成文本“禁用”；鞋品业务禁填规则优先，留空 |
| 81 | 平车针距12~14针/3cm | 否 | 否 |
| 82 | AQL抽检标准 | 无 | 无 |
| 85 | 货源类别 | 现货 | 现货；数据库fixed分支优先于普通来源派生 |
| 96 | 是否新品 | 是 | 是 |
| 184 | 发货方式 | 快递 | 快递发货 |

“禁用”在本次本地来源解析/字段归一中是字段值，不等于直接删除必填字段；远端如何展示尚未验证。本轮不是完整发布链路复测。

## 仍有证据边界的字段

- 安全等级：规则32为manual、无默认值。不能凭商品类目补A/B/C类。
- 服装版型：规则44为manual、无默认值；文案或AI策略仍需当前模板和证据。
- 里料：规则30为skip。但源码对服饰有明确里料来源时可优先采用派生值；不能机械将所有里料清空。107129当前仍无可用值且模板必填。
- 里料材质(多选)：通用域规则49和抖音域规则175并存；后者明确来源为文案表“面料成分”。接入必须复现域筛选和规则顺序。
- 图案、帽型、安全等级、充绒克重、PLM冲突等并未因取得固定规则而自动解决。

## 接入时必须保留的语义

1. `fieldMappingRulesForDraft`先取租户enabled规则；有规则时按商品域筛选，无规则才回退历史表。不能把两表无条件合并。
2. `readSourceValue`先执行业务禁填、强制派生等分支，普通fixed规则采用default_value；价格/来源引用类默认值需解析，不能直接发送说明文字。
3. blocking=false不能解释为允许缺必填字段：最终required/blocking主要来自当前深绘模板和特殊业务必填规则。
4. 本轮将真实数据库规则交给本地Listingify 679042f的纯函数，结合四款已有模板快照检查来源值和归一值；未验证线上部署代码SHA或远端最终payload。

本地证据目录 `.codex-tmp/db-rule-audit/` 包含只读脚本、实时规则快照与逐款解析结果。CLI接入这些数据库规则尚未实施。
