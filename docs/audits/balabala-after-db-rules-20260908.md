# 线上数据库规则接入后的剩余问题

## 已接入范围

CLI内置线上“电商巴拉巴拉 / 1162”209条启用规则的2026-09-08 16:59:45快照。纯本地构建无需连接Listingify数据库或MDM接口；以后线上规则变化需要更新快照。构建审计记录快照版本。

保留当前模板过滤、必填校验、人工/审计覆盖、业务禁填、结构尺码专用管线、本地MDM一致吊牌价、合法成分等专用处理。普通固定值先于普通派生兜底；来源映射支持按文案/上市计划/MDM寻找真实列。按精确字段名和商品域选择规则，同名多条按数据库ID顺序后者优先。不把说明文字直接当商品值。

已解除的规则缺项：尺寸推荐表=禁用、服饰试穿报告表=禁用、针距=否、AQL=无、货源类别=现货、是否新品=是、发货方式=快递发货。并接入其他fixed/skip/source映射，例如是否申请新品、供货方式、京东发货地等。鞋品试穿报告表仍按业务禁填留空。鞋品尺码类型保留欧码规则，不被通用“身型”覆盖；经审计的本地生产企业事实保持优先。这些明确例外不应描述成与数据库逐字相等。

## 完整本地重建后的剩余阻断

本次调用WorkflowEngine.assemble，包含普通字段、真实尺码构建、充绒联动。使用四款正式资料快照，清除普通人工/AI旧值，仅保留用户授权的四个特殊字段覆盖。不是仅运行普通字段构建函数。

| sourceSpu | 剩余阻断 | 后续处理方向 |
| --- | --- | --- |
| 204426140121 | 适用场景(多选)、厚薄、鞋帮高度 | 场景来源“休闲”未命中当前枚举；厚薄/鞋帮需文案或本地图包证据归一 |
| 202426107129 | 面料及材质成分组、里料、安全等级、逐码充绒量及填充/绒子比例、是否带帽、流行元素(多选)、PLM量点冲突 | 原成分栏“缺水洗”不是成分事实；核对图包吊牌/洗唛；两件套量点需拆清，不可混用 |
| 202426108035 | 安全等级 | 数据库规则为manual且无默认值；从吊牌/洗唛或已确认事实补充 |
| 202426121024 | 尺码表、安全等级、版型、图案、是否带帽、里料材质(多选) | 插肩袖表头/肩宽量点适配；版型、里料原文与模板枚举核对；图案/帽型等用文案与图包证据 |

107129还有未由当前校验自动阻断的来源冲突：计划“短款羽绒服”，文案“羽绒马甲两件套”；需纳入下一轮来源冲突处理。不得因字段枚举合法便宣称款式事实正确。

## 剩余验证边界

- 数据库来源规则接入不等于自动完成OCR/AI事实审计；现有图片仍需走可追溯证据流程。
- 尚未证明Listingify全部后置规则/平台结构与CLI最终payload完全相等。旧108条纯函数差异候选不作为本次剩余缺陷数量。
- 天猫导购标题和京东子属性新生成格式未做远端验收；四个特殊字段继续保留用户授权的原值。
- 鞋品此前未回读到单位、材质(AKC)、原产国(AKC)、品牌(文本)、品牌(单选)；销售尺码备注和25实拍文案顺序待UI核验。本轮未重新请求远端。
- 缺尺码的107128、107033、108104仍按用户要求不进入发布范围。

完整逐字段对照：`balabala-after-db-rules-fields.csv`，包括品类、字段、来源、当前值、缺失处理、状态和发布阶段。原始重建审计保存在忽略目录 `.codex-tmp/db-rule-audit/rebuilt/`。

测试：239项通过，lint/build/diff检查通过。未发布任何商品，未修改Listingify数据库，未提交Git。
